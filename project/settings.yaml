default:

  VERSION: 0.0.1

  DEBUG: False

  # settings know their environment, this is set when dynaconf is initialized
  ENVIRONMENT:

  # the secret key must be set in each environment
  SECRET_KEY:

  # there is no default set for ALLOWED_HOSTS since this depends on the environment
  ALLOWED_HOSTS: []

  # add our installed apps
  INSTALLED_APPS:
    - django.contrib.admin
    - django.contrib.auth
    - django.contrib.contenttypes
    - django.contrib.sessions
    - django.contrib.messages
    - django.contrib.staticfiles
    - app.apps.AppConfig
#    - dynaconf_merge  # IMPORTANT

  MIDDLEWARE:
    - django.middleware.security.SecurityMiddleware
    - django.contrib.sessions.middleware.SessionMiddleware
    - django.middleware.common.CommonMiddleware
    - django.middleware.csrf.CsrfViewMiddleware
    - django.contrib.auth.middleware.AuthenticationMiddleware
    - django.contrib.messages.middleware.MessageMiddleware
    - django.middleware.clickjacking.XFrameOptionsMiddleware

  ROOT_URLCONF: project.urls

  # standard Django configuration
  TEMPLATES:
    -
      BACKEND: django.template.backends.django.DjangoTemplates
      DIRS: [ ]
      APP_DIRS: True
      OPTIONS:
        context_processors:
          - django.template.context_processors.debug
          - django.template.context_processors.media
          - django.template.context_processors.request
          - django.contrib.auth.context_processors.auth
          - django.contrib.messages.context_processors.messages

  # Password validation
  # https://docs.djangoproject.com/en/2.0/ref/settings/#auth-password-validators
  AUTH_PASSWORD_VALIDATORS:
    - NAME: django.contrib.auth.password_validation.UserAttributeSimilarityValidator
    - NAME: django.contrib.auth.password_validation.MinimumLengthValidator
    - NAME: django.contrib.auth.password_validation.CommonPasswordValidator
    - NAME: django.contrib.auth.password_validation.NumericPasswordValidator

  WSGI_APPLICATION: project.wsgi.application

  # use timezone and english language
  LANGUAGE_CODE: en-us
  TIME_ZONE: UTC
  USE_I18N: True
  USE_L10N: True
  USE_TZ: True

  # standard Django configuration
  STATIC_URL: /static/
  STATIC_ROOT:

  # use postgres instead of sqlite
  DATABASES:
    default:
      ENGINE: django.db.backends.postgresql
      HOST: postgres
      NAME: postgres
      USER: postgres
      PASSWORD: postgres
      PORT: 5432

dev:
  DEBUG: True
  SECRET_KEY: SECRET-KEY-FOR-DEV-ENVIRONMENT
  ALLOWED_HOSTS:
    - localhost
    - 127.0.0.1
